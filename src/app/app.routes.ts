import { Routes } from '@angular/router';
import { CountrySearchComponent } from './components/country-search/country-search.component';

export const routes: Routes = [{ path: '', component: CountrySearchComponent }];
